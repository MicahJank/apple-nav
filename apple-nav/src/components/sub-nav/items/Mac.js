import React from 'react';

import { NavLink } from 'react-router-dom';

import styled from 'styled-components';


const Mac = (props) => {

    return (
        <div>
            Mac
        </div>
    );
};

export default Mac;