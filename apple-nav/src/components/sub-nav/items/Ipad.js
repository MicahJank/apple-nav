import React from 'react';

import { NavLink } from 'react-router-dom';

import styled from 'styled-components';


const Ipad = (props) => {

    return (
        <div>
            Ipad
        </div>
    );
};

export default Ipad;